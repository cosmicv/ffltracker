# FFLTracker

React and SQLite application for tracking friends-and-family loans.

## Development

```bash
npm install
npm run dev
```

The web application runs at `http://localhost:5173` and the API at
`http://127.0.0.1:3001`.

See `CLAUDE.md` for database import and environment configuration.
